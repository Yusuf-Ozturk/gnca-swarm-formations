# Python environment / caches (never committed)
.venv/
__pycache__/
*.pyc

# NOTE: generated artifacts (checkpoints in runs/, results/ summaries, charts and
# animations) ARE committed on purpose, so the repo ships with the study's data.
# Regenerate them with:
#   python train.py --shape <shape> --perception <cone|circular>   (once per pair)
#   python results.py

# Claude Code session-local runtime state
.claude/scheduled_tasks.lock
